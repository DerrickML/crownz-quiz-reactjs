const initialState = {
    // Define initial state
  };
  
  const quizReducer = (state = initialState, action) => {
    switch (action.type) {
      // Handle actions
      default:
        return state;
    }
  };
  
  export default quizReducer;